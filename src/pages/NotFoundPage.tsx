import { EmptyState } from '../components/ui/EmptyState.tsx'

export function NotFoundPage() {
  return <EmptyState eyebrow="Page not found" title="Let’s find your way back." description="This address doesn’t lead to a page. Head back to Describe It! to get started." />
}
